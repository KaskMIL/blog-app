<section class='user-profile-container'>

  <article class='user-container'>
    <img src=<%= @user.photo %> alt="Image placeholder">
    <div class='user-data'>
      <h1><%= @user.name %></h1>
      <p>Posts: <%= @user.postCounter %></p>
    </div>
  </article>

  <section class='user-page'>
    <article class='bio'>
      <h3>Bio</h3>
      <p><%= @user.bio %></p>
    </article>

    <% @last_posts.each do |post|%>
      <article class='user-post'>
        <h3><%= post.title %></h3>
        <p><%= post.text %></p>
        <span class='counters'>Comments: <%= post.commentsCounter %> - Likes: <%= post.likesCounter %></span>
      </article>
    <% end %>

    <a class="posts-btn" href="/users/<%=@user.id%>/posts">See all posts</a>
  </section>
</section>
